import React, {useState, useEffect} from 'react';
import './Board.scss'

const Board = () => {
    const [userList, setUserList] = useState([])

    useEffect(() => {

    }, [])

    return (
        <div className="board">
            <h1>User Board</h1>
        </div>
    );
};


export default Board;
